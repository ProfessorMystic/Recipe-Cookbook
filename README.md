# Recipe-Cookbook
Recipe Cookbook is a full-stack web application that lets users create, browse, update, and delete recipes. It’s built with a FastAPI backend (Python) and a lightweight HTML/JavaScript frontend, with data stored in an SQLite database for persistence.
